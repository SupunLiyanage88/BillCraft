import { Typography, Box } from "@mui/material";

function Template() {
  return (
    <Box sx={{ padding: 4 }}>
      <Typography variant="h4" gutterBottom>
        Templates
      </Typography>
      <Typography variant="body1" gutterBottom>
        Browse and select from our collection of invoice templates.
      </Typography>
    </Box>
  );
}

export default Template;
